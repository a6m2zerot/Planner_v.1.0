#  In Case of db ERROR

"""from app.models import StaticTasks
from app import db

a = StaticTasks(status="Отменено")
b = StaticTasks(status="Выполнено")
c = StaticTasks(status="Перенесено")
db.session.add_all([a, b, c])
db.session.commit()"""
